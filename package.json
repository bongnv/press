{
  "scripts": {
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,vue,json,md,css}\"",
    "lint": "lerna run lint --stream",
    "test": "lerna run test --stream",
    "watch": "lerna run watch --stream --no-sort"
  },
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "name": "press"
}
